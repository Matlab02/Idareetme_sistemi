/* Azerbaijan tax-aware management report. Final declarations require accountant approval. */
(() => {
  const TAX_RATE = 0.20;
  const number = (value) => { const parsed = Number(String(value ?? 0).replace(/\s/g, "").replace(",", ".")); return Number.isFinite(parsed) ? parsed : 0; };
  const money = (value) => new Intl.NumberFormat("az-AZ", { style: "currency", currency: "AZN", minimumFractionDigits: 2, maximumFractionDigits: 2 }).format(number(value));
  const esc = (value) => String(value ?? "").replace(/[&<>'"]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#039;", '"': "&quot;" }[char]));
  const dateText = (value) => { const date = new Date(value); return Number.isNaN(date.getTime()) ? String(value || "—") : date.toLocaleDateString("az-AZ"); };
  const db = () => window.db || null;
  const save = () => { if (typeof window.save === "function") window.save(); };
  const audit = (action, entity) => { if (typeof window.audit === "function") window.audit(action, entity); };
  const toast = (message) => { if (typeof window.toast === "function") window.toast(message); };
  const expenseTypes = ["Market xərci", "Azpetrol / yanacaq", "Bank xərci", "Maaş", "Logistika", "İcarə", "Digər xərc"];
  let selectedMonth = new Date().toISOString().slice(0, 7);

  function style() {
    if (document.querySelector("#reports-module-styles")) return;
    const tag = document.createElement("style"); tag.id = "reports-module-styles";
    tag.textContent = `.report-metrics{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:12px;margin:20px 0}.report-metric{padding:16px;border:1px solid #dfe7f2;border-radius:14px;background:linear-gradient(145deg,#fff,#f8fbff);box-shadow:0 8px 22px rgba(25,55,95,.045)}.report-metric span{display:block;color:#74839a;font-size:10px;font-weight:750;text-transform:uppercase;letter-spacing:.04em}.report-metric strong{display:block;margin-top:8px;font-size:18px;color:#10284b}.report-metric.sales{border-top:3px solid #17a36d}.report-metric.purchase{border-top:3px solid #4d83ce}.report-metric.profit{border-top:3px solid #2d8cde}.report-metric.deductible{border-top:3px solid #14a178}.report-metric.tax{border-top:3px solid #a661d4}.report-metric.nondeductible{border-top:3px solid #df8a25}.report-metric.net{border-top:3px solid #3d72d6}.report-metric.negative{border-top:3px solid #db465a}.report-metric.negative strong{color:#bf3148}.report-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.report-panel,.report-expenses{overflow:hidden}.report-panel .panel-head,.report-expenses .panel-head{display:flex;justify-content:space-between;align-items:flex-start;gap:14px;padding:18px 18px 12px}.report-panel h2,.report-expenses h2{margin:0;font-size:15px}.report-panel p,.report-expenses p{margin:5px 0 0;color:#718098;font-size:12px}.report-panel .panel-head>b,.report-expenses .panel-head>b{color:#153d73;font-size:14px;white-space:nowrap}.report-expenses{margin-top:16px}.report-amount{text-align:right;font-weight:750;color:#153d73;white-space:nowrap}.report-profit{color:#06855d;font-weight:750;white-space:nowrap}.report-expense-amount{color:#bc5c10}.report-type,.report-tax-status{display:inline-flex;padding:5px 9px;border-radius:999px;font-size:11px;font-weight:750;white-space:nowrap}.report-type{background:#fff2df;color:#aa650d}.report-tax-status.deductible{background:#e8f8f0;color:#087756}.report-tax-status.nondeductible{background:#f5edfF;color:#7550a3}.report-actions{display:flex;align-items:end;justify-content:flex-end;gap:8px;flex-wrap:wrap}.report-month{display:grid;gap:4px;color:#738299;font-size:10px;font-weight:750;text-transform:uppercase;letter-spacing:.04em}.report-month input{height:39px;padding:8px 10px;border:1px solid #d9e3ef;border-radius:10px;background:#fff;color:#173051;font:inherit;text-transform:none}.report-actions .secondary,.report-actions .primary{height:39px;white-space:nowrap}.report-tax-note{margin:0 0 16px;padding:13px 15px;border-left:3px solid #527fca;border-radius:9px;background:#f4f8ff;color:#506681;font-size:12px;line-height:1.55}.finance-dashboard-panel{margin:19px 0 4px;padding:18px;border:1px solid #dce7f5;border-radius:16px;background:linear-gradient(120deg,#f7fbff,#fff)}.finance-dashboard-head{display:flex;align-items:center;justify-content:space-between;gap:14px}.finance-dashboard-head h2{margin:0;font-size:16px;color:#14335e}.finance-dashboard-head p{margin:4px 0 0;color:#6b7e96;font-size:12px}.finance-dashboard-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.finance-dashboard-actions input{height:36px;border:1px solid #d6e1ef;border-radius:9px;padding:7px 9px;background:#fff;font:inherit}.finance-dashboard-panel .report-metrics{margin:15px 0 0}.finance-dashboard-panel .report-metric{background:#fff}.report-expense-modal .modal{width:min(700px,100%)}@media(max-width:860px){.report-grid{grid-template-columns:1fr}.report-actions{justify-content:flex-start;margin-top:16px}.finance-dashboard-head{align-items:flex-start;flex-direction:column}}@media(max-width:540px){.report-actions{display:grid;grid-template-columns:1fr 1fr;width:100%}.report-month{grid-column:1/-1}.report-month input,.report-actions button{width:100%}.report-panel .panel-head,.report-expenses .panel-head{align-items:flex-start;flex-direction:column}.finance-dashboard-actions{width:100%}.finance-dashboard-actions input,.finance-dashboard-actions button{width:100%}}`;
    document.head.append(tag);
  }
  function state() { const value = db(); if (!value) return null; value.incomingInvoices ||= []; value.outgoingInvoices ||= []; value.additionalExpenses ||= []; return value; }
  const isMonth = (value, month) => { const date = new Date(value); return !Number.isNaN(date.getTime()) && date.toISOString().slice(0, 7) === month; };
  const monthLabel = (month) => new Date(`${month}-01T12:00:00`).toLocaleDateString("az-AZ", { month: "long", year: "numeric" });
  const netInvoice = (invoice) => Number.isFinite(Number(invoice?.subtotal)) ? number(invoice.subtotal) : Math.max(0, number(invoice?.total) - number(invoice?.vat));
  function outgoingCost(invoice) {
    const sales = netInvoice(invoice);
    if (Number.isFinite(Number(invoice?.profit))) return Math.max(0, sales - number(invoice.profit));
    return (invoice?.items || []).reduce((sum, item) => sum + (item.allocations || []).reduce((line, lot) => line + number(lot.quantity) * number(lot.unitCost), 0), 0);
  }
  function currentReport(value, month = selectedMonth) {
    return { incomingInvoices: value.incomingInvoices.filter((item) => isMonth(item.date, month)), outgoingInvoices: value.outgoingInvoices.filter((item) => isMonth(item.date, month)), additionalExpenses: value.additionalExpenses.filter((item) => isMonth(item.date, month)) };
  }
  function totals(report) {
    const sales = report.outgoingInvoices.reduce((sum, item) => sum + netInvoice(item), 0);
    const purchases = report.incomingInvoices.reduce((sum, item) => sum + netInvoice(item), 0);
    const cogs = report.outgoingInvoices.reduce((sum, item) => sum + outgoingCost(item), 0);
    const grossProfit = sales - cogs;
    const deductible = report.additionalExpenses.filter((item) => item.taxStatus === "DEDUCTIBLE").reduce((sum, item) => sum + number(item.amount), 0);
    const nonDeductible = report.additionalExpenses.filter((item) => item.taxStatus !== "DEDUCTIBLE").reduce((sum, item) => sum + number(item.amount), 0);
    const taxableProfit = grossProfit - deductible;
    const incomeTax = Math.max(0, taxableProfit) * TAX_RATE;
    const outputVat = report.outgoingInvoices.reduce((sum, item) => sum + number(item.vat), 0);
    const inputVat = report.incomingInvoices.reduce((sum, item) => sum + number(item.vat), 0);
    return { sales, purchases, cogs, grossProfit, deductible, nonDeductible, taxableProfit, incomeTax, outputVat, inputVat, vatDifference: outputVat - inputVat, net: grossProfit - deductible - nonDeductible - incomeTax };
  }
  const metric = (label, amount, type = "") => `<article class="report-metric ${type}"><span>${label}</span><strong>${money(amount)}</strong></article>`;
  const taxLabel = (item) => item.taxStatus === "DEDUCTIBLE" ? '<span class="report-tax-status deductible">Çıxılan · sənədli</span>' : '<span class="report-tax-status nondeductible">Çıxılmayan / yoxlanılır</span>';
  function mountFinanceDashboard() {
    const active = document.querySelector('.nav button[data-page="dash"].active');
    const root = document.querySelector("#root"), content = root?.querySelector(".content");
    if (!active || !content || content.querySelector(".finance-dashboard-panel")) return;
    const value = state(); if (!value) return;
    const summary = totals(currentReport(value));
    const panel = document.createElement("section"); panel.className = "finance-dashboard-panel";
    panel.innerHTML = `<div class="finance-dashboard-head"><div><h2>Aylıq maliyyə və vergi xülasəsi</h2><p>${esc(monthLabel(selectedMonth))} · qaimələr və sənədli xərclər əsasında</p></div><div class="finance-dashboard-actions"><input class="dashboard-finance-month" type="month" value="${selectedMonth}"><button class="secondary dashboard-open-reports">Ətraflı hesabat →</button></div></div><div class="report-metrics">${metric("Satış gəliri · ƏDV-siz", summary.sales, "sales")}${metric("Brüt mənfəət", summary.grossProfit, "profit")}${metric("Vergitutma mənfəəti", summary.taxableProfit, "profit")}${metric("Mənfəət vergisi · 20%", summary.incomeTax, "tax")}${metric("Çıxılmayan xərclər", summary.nonDeductible, "nondeductible")}${metric("Xalis nəticə", summary.net, summary.net < 0 ? "negative" : "net")}</div>`;
    const cards = content.querySelector(".cards"); if (cards) cards.insertAdjacentElement("afterend", panel); else content.append(panel);
  }

  const download = (blob, filename) => { const link = document.createElement("a"); link.href = URL.createObjectURL(blob); link.download = filename; link.click(); setTimeout(() => URL.revokeObjectURL(link.href), 1000); };
  const xml = (value) => esc(value).replace(/\n/g, "&#10;");
  const col = (index) => { let output = ""; for (let current = index + 1; current; current = Math.floor((current - 1) / 26)) output = String.fromCharCode(65 + ((current - 1) % 26)) + output; return output; };
  const crc32 = (bytes) => { let crc = -1; for (const byte of bytes) { crc ^= byte; for (let bit = 0; bit < 8; bit += 1) crc = (crc >>> 1) ^ (0xedb88320 & -(crc & 1)); } return (crc ^ -1) >>> 0; };
  const zip = (entries) => { const encoder = new TextEncoder(), chunks = [], central = []; let offset = 0; const p16 = (list, value) => list.push(value & 255, (value >>> 8) & 255), p32 = (list, value) => list.push(value & 255, (value >>> 8) & 255, (value >>> 16) & 255, (value >>> 24) & 255); entries.forEach(([name, text]) => { const body = encoder.encode(text), filename = encoder.encode(name), sum = crc32(body), local = []; p32(local, 0x04034b50); p16(local, 20); p16(local, 0x0800); p16(local, 0); p16(local, 0); p16(local, 0); p32(local, sum); p32(local, body.length); p32(local, body.length); p16(local, filename.length); p16(local, 0); local.push(...filename, ...body); chunks.push(new Uint8Array(local)); const record = []; p32(record, 0x02014b50); p16(record, 20); p16(record, 20); p16(record, 0x0800); p16(record, 0); p16(record, 0); p16(record, 0); p32(record, sum); p32(record, body.length); p32(record, body.length); p16(record, filename.length); p16(record, 0); p16(record, 0); p16(record, 0); p16(record, 0); p32(record, 0); p32(record, offset); record.push(...filename); central.push(new Uint8Array(record)); offset += local.length; }); const size = central.reduce((sum, item) => sum + item.length, 0), end = []; p32(end, 0x06054b50); p16(end, 0); p16(end, 0); p16(end, entries.length); p16(end, entries.length); p32(end, size); p32(end, offset); p16(end, 0); return new Blob([...chunks, ...central, new Uint8Array(end)], { type: "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" }); };
  function exportExcel(report, summary) {
    const rows = [["Aylıq vergi və maliyyə hesabatı"], ["Dövr", monthLabel(selectedMonth)], [], ["Göstərici", "Məbləğ (AZN)"], ["Satış gəliri · ƏDV-siz", summary.sales], ["Alış qaimələri · ƏDV-siz (məlumat)", summary.purchases], ["Satılan malın maya dəyəri", summary.cogs], ["Brüt mənfəət", summary.grossProfit], ["Gəlirdən çıxılan xərclər", summary.deductible], ["Vergitutma mənfəəti", summary.taxableProfit], ["Mənfəət vergisi · 20%", summary.incomeTax], ["Gəlirdən çıxılmayan xərclər", summary.nonDeductible], ["Xalis nəticə", summary.net], ["Çıxış ƏDV", summary.outputVat], ["Giriş ƏDV", summary.inputVat], ["ƏDV fərqi · məlumat", summary.vatDifference], [], ["Əlavə xərclər"], ["Növ", "Açıqlama", "Sənəd", "Vergi statusu", "Məbləğ"]];
    report.additionalExpenses.forEach((item) => rows.push([item.type, item.note || "—", item.documentRef || "—", item.taxStatus === "DEDUCTIBLE" ? "Çıxılan" : "Çıxılmayan / yoxlanılır", number(item.amount)]));
    const cells = (row, index) => row.map((value, cellIndex) => `<c r="${col(cellIndex)}${index + 1}"${typeof value === "number" ? "" : ' t="inlineStr"'}>${typeof value === "number" ? `<v>${value}</v>` : `<is><t xml:space="preserve">${xml(value)}</t></is>`}</c>`).join("");
    const sheet = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><cols><col min="1" max="1" width="31" customWidth="1"/><col min="2" max="2" width="35" customWidth="1"/><col min="3" max="4" width="23" customWidth="1"/><col min="5" max="5" width="18" customWidth="1"/></cols><sheetData>${rows.map((row, index) => `<row r="${index + 1}">${cells(row, index)}</row>`).join("")}</sheetData></worksheet>`;
    const styles = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><styleSheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><fonts count="1"><font><sz val="11"/><name val="Arial"/></font></fonts><fills count="2"><fill><patternFill patternType="none"/></fill><fill><patternFill patternType="gray125"/></fill></fills><borders count="1"><border><left/><right/><top/><bottom/></border></borders><cellStyleXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0"/></cellStyleXfs><cellXfs count="1"><xf numFmtId="0" fontId="0" fillId="0" borderId="0" xfId="0"/></cellXfs></styleSheet>`;
    const workbook = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><workbook xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main" xmlns:r="http://schemas.openxmlformats.org/officeDocument/2006/relationships"><sheets><sheet name="Aylıq hesabat" sheetId="1" r:id="rId1"/></sheets></workbook>`, rels = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="xl/workbook.xml"/></Relationships>`, workbookRels = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships"><Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/worksheet" Target="worksheets/sheet1.xml"/><Relationship Id="rId2" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/styles" Target="styles.xml"/></Relationships>`, types = `<?xml version="1.0" encoding="UTF-8" standalone="yes"?><Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types"><Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/><Default Extension="xml" ContentType="application/xml"/><Override PartName="/xl/workbook.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet.main+xml"/><Override PartName="/xl/worksheets/sheet1.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.worksheet+xml"/><Override PartName="/xl/styles.xml" ContentType="application/vnd.openxmlformats-officedocument.spreadsheetml.styles+xml"/></Types>`;
    download(zip([["[Content_Types].xml", types], ["_rels/.rels", rels], ["xl/workbook.xml", workbook], ["xl/_rels/workbook.xml.rels", workbookRels], ["xl/styles.xml", styles], ["xl/worksheets/sheet1.xml", sheet]]), `ayliq-vergi-hesabati-${selectedMonth}.xlsx`);
  }
  function exportPdf(report, summary) {
    const title = `Aylıq vergi və maliyyə hesabatı · ${monthLabel(selectedMonth)}`;
    const expenseRows = report.additionalExpenses.map((item) => `<tr><td>${esc(item.type)}</td><td>${esc(item.note || "—")}</td><td>${esc(item.documentRef || "—")}</td><td>${item.taxStatus === "DEDUCTIBLE" ? "Çıxılan" : "Çıxılmayan / yoxlanılır"}</td><td>${money(item.amount)}</td></tr>`).join("") || '<tr><td colspan="5">Bu ay üçün əlavə xərc yoxdur.</td></tr>';
    const popup = window.open("", "_blank"); if (!popup) return toast("PDF pəncərəsi bloklanıb. Brauzerdə popup icazəsini açın.");
    popup.document.write(`<!doctype html><html lang="az"><head><meta charset="utf-8"><title>${esc(title)}</title><style>*{box-sizing:border-box}body{margin:0;background:#eef3f9;color:#142846;font-family:Arial,Helvetica,sans-serif}.sheet{width:210mm;min-height:297mm;margin:12px auto;padding:15mm;background:#fff;box-shadow:0 16px 40px #18385d22}header{border-bottom:2px solid #1f4d87;padding-bottom:12px}header span{color:#407ad3;font-size:10px;font-weight:700;letter-spacing:.1em}h1{font-size:23px;margin:7px 0}p{color:#60758e;font-size:11px;line-height:1.5}.summary{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin:18px 0}.summary div{border:1px solid #dce5ef;border-radius:8px;background:#f8fbff;padding:9px}.summary small{display:block;color:#748297;font-size:9px}.summary b{display:block;margin-top:5px;font-size:12px}h2{font-size:14px;margin:22px 0 8px}table{width:100%;border-collapse:collapse;font-size:10px}th{background:#183f75;color:#fff;text-align:left;padding:7px}td{padding:7px;border-bottom:1px solid #dfe6ef}td:last-child{text-align:right;font-weight:700}@media print{body{background:#fff}.sheet{margin:0;box-shadow:none;width:210mm}@page{size:A4 portrait;margin:0}}</style></head><body><main class="sheet"><header><span>MALİYYƏ VƏ VERGİ HESABATI</span><h1>${esc(title)}</h1><p>İdarəetmə məqsədli ilkin hesablamadır. Mənfəət vergisi ƏDV-siz satış gəlirindən satılan malın maya dəyəri və sənədlə təsdiqlənən, gəlirin əldə edilməsi ilə bağlı xərclər çıxıldıqdan sonra 20% ilə hesablanır. Bəyannamə verilməzdən əvvəl mühasib tərəfindən yoxlanmalıdır.</p></header><section class="summary"><div><small>Satış gəliri · ƏDV-siz</small><b>${money(summary.sales)}</b></div><div><small>Maya dəyəri</small><b>${money(summary.cogs)}</b></div><div><small>Brüt mənfəət</small><b>${money(summary.grossProfit)}</b></div><div><small>Çıxılan xərclər</small><b>${money(summary.deductible)}</b></div><div><small>Vergitutma mənfəəti</small><b>${money(summary.taxableProfit)}</b></div><div><small>Mənfəət vergisi · 20%</small><b>${money(summary.incomeTax)}</b></div><div><small>Çıxılmayan xərclər</small><b>${money(summary.nonDeductible)}</b></div><div><small>Xalis nəticə</small><b>${money(summary.net)}</b></div><div><small>ƏDV fərqi · məlumat</small><b>${money(summary.vatDifference)}</b></div></section><h2>Əlavə xərclər və vergi statusu</h2><table><thead><tr><th>Növ</th><th>Açıqlama</th><th>Sənəd</th><th>Status</th><th>Məbləğ</th></tr></thead><tbody>${expenseRows}</tbody></table></main><script>window.onload=()=>window.print()<\/script></body></html>`);
    popup.document.close(); popup.opener = null;
  }
  function renderReports() {
    style(); const value = state(); if (!value) return; const report = currentReport(value), summary = totals(report), incoming = [...report.incomingInvoices].sort((a, b) => new Date(b.date) - new Date(a.date)), outgoing = [...report.outgoingInvoices].sort((a, b) => new Date(b.date) - new Date(a.date)), expenses = [...report.additionalExpenses].sort((a, b) => new Date(b.date) - new Date(a.date));
    document.querySelectorAll(".nav button[data-page]").forEach((button) => button.classList.toggle("active", button.dataset.page === "reports"));
    const root = document.querySelector("#root"); if (!root) return;
    root.innerHTML = `<div class="content report-page"><div class="head"><div><div class="eyebrow">MALİYYƏ · VERGİ UÇOTU</div><h1>Hesabatlar</h1><p>${esc(monthLabel(selectedMonth))} üzrə qaimə, xərc və vergi uçotu.</p></div><div class="report-actions"><label class="report-month"><span>Hesabat ayı</span><input class="report-month-input" type="month" value="${selectedMonth}"></label><button class="secondary report-download-excel">↓ Excel</button><button class="secondary report-download-pdf">↓ PDF</button><button class="primary report-add-expense">＋ Əlavə xərc</button></div></div><div class="report-tax-note"><b>Hesablama qaydası:</b> Mənfəət vergisi = (ƏDV-siz satış gəliri − satılan malın maya dəyəri − <em>sənədli, fəaliyyətlə bağlı xərclər</em>) × 20%. Market və Azpetrol xərclərini əlavə edərkən onların vergi statusunu seçin; sənədsiz və ya əlaqəsi təsdiqlənməyən xərc vergi bazasından çıxılmır.</div><section class="report-metrics">${metric("Satış gəliri · ƏDV-siz", summary.sales, "sales")}${metric("Alış qaimələri · ƏDV-siz", summary.purchases, "purchase")}${metric("Satılan malın maya dəyəri", summary.cogs, "purchase")}${metric("Brüt mənfəət", summary.grossProfit, "profit")}${metric("Çıxılan xərclər", summary.deductible, "deductible")}${metric("Vergitutma mənfəəti", summary.taxableProfit, "profit")}${metric("Mənfəət vergisi · 20%", summary.incomeTax, "tax")}${metric("Çıxılmayan xərclər", summary.nonDeductible, "nondeductible")}${metric("Xalis nəticə", summary.net, summary.net < 0 ? "negative" : "net")}</section><section class="report-grid"><div class="panel report-panel"><div class="panel-head"><div><h2>Gələn qaimələr</h2><p>${incoming.length} qaimə · ƏDV-siz məbləğ</p></div><b>${money(summary.purchases)}</b></div><div class="table-wrap"><table class="tbl"><thead><tr><th>Qaimə</th><th>Təchizatçı</th><th>Tarix</th><th>ƏDV-siz</th><th>ƏDV</th></tr></thead><tbody>${incoming.map((item) => `<tr><td><b>${esc(item.number)}</b></td><td>${esc(item.supplier || "—")}</td><td>${dateText(item.date)}</td><td class="report-amount">${money(netInvoice(item))}</td><td>${money(item.vat)}</td></tr>`).join("") || '<tr><td colspan="5" class="empty">Bu ay üçün gələn qaimə yoxdur.</td></tr>'}</tbody></table></div></div><div class="panel report-panel"><div class="panel-head"><div><h2>Gedən qaimələr</h2><p>${outgoing.length} qaimə · ƏDV-siz məbləğ</p></div><b>${money(summary.sales)}</b></div><div class="table-wrap"><table class="tbl"><thead><tr><th>Qaimə</th><th>Müştəri</th><th>Tarix</th><th>ƏDV-siz</th><th>Maya</th></tr></thead><tbody>${outgoing.map((item) => `<tr><td><b>${esc(item.number)}</b></td><td>${esc(item.customer || "—")}</td><td>${dateText(item.date)}</td><td class="report-amount">${money(netInvoice(item))}</td><td>${money(outgoingCost(item))}</td></tr>`).join("") || '<tr><td colspan="5" class="empty">Bu ay üçün gedən qaimə yoxdur.</td></tr>'}</tbody></table></div></div></section><section class="panel report-expenses"><div class="panel-head"><div><h2>Əlavə xərclər</h2><p>Vergi statusunu sənəd və fəaliyyət əlaqəsinə görə seçin.</p></div><b>${money(summary.deductible + summary.nonDeductible)}</b></div><div class="table-wrap"><table class="tbl"><thead><tr><th>Xərc növü</th><th>Açıqlama</th><th>Sənəd / çek / e-qaimə</th><th>Vergi statusu</th><th>Məbləğ</th><th></th></tr></thead><tbody>${expenses.map((item) => `<tr><td><span class="report-type">${esc(item.type)}</span></td><td>${esc(item.note || "—")}</td><td>${esc(item.documentRef || "—")}</td><td>${taxLabel(item)}</td><td class="report-amount report-expense-amount">${money(item.amount)}</td><td><button class="secondary report-delete-expense" data-expense-id="${esc(item.id)}">Sil</button></td></tr>`).join("") || '<tr><td colspan="6" class="empty">Bu ay üçün əlavə xərc qeydə alınmayıb.</td></tr>'}</tbody></table></div></section></div>`;
  }
  function openExpense() {
    const host = document.createElement("div"); host.className = "modal-bg report-expense-modal";
    host.innerHTML = `<div class="modal"><div class="documents-head"><div><div class="eyebrow">MALİYYƏ · VERGİ STATUSU</div><h2>Əlavə xərc əlavə et</h2><p>Market və Azpetrol xərcləri də burada yazılır. Vergi statusunu sənədə və biznes əlaqəsinə görə seçin.</p></div><button class="icon" data-expense-close>×</button></div><form id="reportExpenseForm" class="form"><div class="field"><label>Xərc növü</label><select name="type">${expenseTypes.map((type) => `<option>${type}</option>`).join("")}</select></div><div class="field"><label>Tarix</label><input name="date" type="date" value="${new Date().toISOString().slice(0, 10)}" required></div><div class="field"><label>Məbləğ (AZN)</label><input name="amount" type="number" step="0.01" min="0.01" required placeholder="0.00"></div><div class="field"><label>Vergi statusu</label><select name="taxStatus"><option value="NON_DEDUCTIBLE">Vergi bazasına daxil etmə</option><option value="DEDUCTIBLE">Gəlirdən çıxılan xərc</option></select></div><div class="field full"><label>Sənəd / çek / e-qaimə nömrəsi</label><input name="documentRef" placeholder="Məsələn: e-QF-12345 və ya çek №"></div><div class="field full"><label>Açıqlama</label><input name="note" placeholder="Xərcin biznes fəaliyyəti ilə əlaqəsini qısa yazın"></div></form><div class="actions"><button class="secondary" data-expense-close>Ləğv et</button><button class="primary" data-expense-save>Xərci əlavə et</button></div></div>`;
    host.onclick = (event) => { if (event.target === host || event.target.closest("[data-expense-close]")) host.remove(); };
    host.querySelector("[data-expense-save]").onclick = () => { const values = Object.fromEntries(new FormData(host.querySelector("#reportExpenseForm"))), amount = number(values.amount); if (!values.type || amount <= 0) return toast("Xərc növü və düzgün məbləğ yazın."); if (values.taxStatus === "DEDUCTIBLE" && !String(values.documentRef || "").trim()) return toast("Gəlirdən çıxılan xərc üçün sənəd, çek və ya e-qaimə nömrəsi yazın."); const value = state(); if (!value) return; value.additionalExpenses.unshift({ id: `EXP-${Date.now()}-${Math.random().toString(36).slice(2, 6)}`, type: values.type, note: String(values.note || "").trim(), documentRef: String(values.documentRef || "").trim(), taxStatus: values.taxStatus, amount, date: values.date, createdAt: new Date().toISOString() }); audit("CREATE", `Əlavə xərc · ${values.type} · ${amount.toFixed(2)} AZN`); save(); host.remove(); renderReports(); toast("Əlavə xərc qeydə alındı."); };
    document.body.append(host);
  }
  function removeExpense(id) { const value = state(); const item = value?.additionalExpenses.find((expense) => expense.id === id); if (!item || !confirm(`${item.type} xərcini silmək istəyirsiniz?`)) return; value.additionalExpenses = value.additionalExpenses.filter((expense) => expense.id !== id); audit("DELETE", `Əlavə xərc · ${item.type} · ${number(item.amount).toFixed(2)} AZN`); save(); renderReports(); toast("Xərc silindi."); }
  function mount() { style(); const button = document.querySelector('.nav [data-page="reports"]'); if (button) button.onclick = renderReports; const root = document.querySelector("#root"); if (root) new MutationObserver(() => mountFinanceDashboard()).observe(root, { childList: true, subtree: true }); setTimeout(mountFinanceDashboard, 0); }
  document.addEventListener("DOMContentLoaded", mount);
  document.addEventListener("click", (event) => { if (event.target.closest(".report-add-expense")) openExpense(); if (event.target.closest(".dashboard-open-reports")) renderReports(); if (event.target.closest(".report-download-excel")) { const value = state(); if (value) { const report = currentReport(value), summary = totals(report); exportExcel(report, summary); audit("EXPORT", `Aylıq Excel vergi hesabatı · ${selectedMonth}`); save(); toast("Excel hesabatı endirildi."); } } if (event.target.closest(".report-download-pdf")) { const value = state(); if (value) { const report = currentReport(value), summary = totals(report); exportPdf(report, summary); audit("EXPORT", `Aylıq PDF vergi hesabatı · ${selectedMonth}`); save(); } } const remove = event.target.closest(".report-delete-expense"); if (remove) removeExpense(remove.dataset.expenseId); });
  document.addEventListener("change", (event) => { if (!/^\d{4}-\d{2}$/.test(event.target.value)) return; if (event.target.matches(".report-month-input")) { selectedMonth = event.target.value; renderReports(); } if (event.target.matches(".dashboard-finance-month")) { selectedMonth = event.target.value; event.target.closest(".finance-dashboard-panel")?.remove(); mountFinanceDashboard(); } });
  window.reportsModule = { render: renderReports };
})();
